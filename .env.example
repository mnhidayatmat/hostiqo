APP_NAME="Hostiqo"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=webhook_manager
DB_USERNAME=webhook_user
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local

# IMPORTANT: Queue worker must be running for deployments to work!
# Run: php artisan queue:work
# Run: php artisan schedule:work
QUEUE_CONNECTION=database

# Web Server User Configuration (Optional)
# Auto-detected if not set. Common values:
# - Linux: www-data
# - macOS: www or _www
# - Custom: your web server user
# WEB_SERVER_USER=www-data
# WEB_SERVER_GROUP=www-data

# System Monitoring Configuration
MONITORING_ENABLED=true
MONITORING_INTERVAL_MINUTES=1
MONITORING_RETENTION_HOURS=24
MONITORING_CHART_HOURS=6

CACHE_STORE=database

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Cloudflare DNS Management (Optional)
# Enable to auto-create DNS A records when creating websites
# Get API token from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone:DNS:Edit
CLOUDFLARE_ENABLED=false
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_PROXIED=false

VITE_APP_NAME="${APP_NAME}"
